// export: make this function accessible outside the module
// Also called a named export
export function add(x: number, y: number): number {
    return x + y
}

export function sub(x: number, y: number): number {
    return x - y
}
